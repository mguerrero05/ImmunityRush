# Mini-game background images

Drop a background/scene image for any mini-game here, using these exact names:

- `sprint-bg.png`  — Hospital Sprint (the runner scene). If it's meant to SCROLL,
  make it a wide, seamlessly-tiling strip; if it's a static backdrop, any size is fine.
- `darts-bg.png`   — Vaccine Darts backdrop
- `freeze-bg.png`  — Flu Freeze backdrop
- `memory-bg.png`  — Memory Clinic backdrop

Tips:
- The interactive stuff (runner, darts, cards, items) sits ON TOP, so leave the
  center reasonably clear / not too busy.
- Transparent isn't needed here (these are backgrounds), but it's fine if so.

## memory-bg.png — specifics
The Memory Clinic board is a **portrait phone** (the game frame is 430×860).
- Make it **portrait**, e.g. **900 × 1600** (or larger — bigger is crisper).
- A grid of 12 cards sits on top of it, so keep it **calm / not too busy** — a
  clinic/waiting-room wall, soft SHN-blue tones, maybe a subtle SHN logo. The cards
  still need to pop, so I'll add a soft darkening scrim behind the grid automatically.
- Anything from a photo-style scene to a flat illustrated backdrop works.

Drop one in and tell me — I'll wire it in behind that mini-game and we'll adjust the fit.
