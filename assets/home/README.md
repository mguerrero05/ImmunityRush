# Home / starting screen reference

Drop your starting-page reference image here as **`home-bg.png`**.

I'll use it as the background of the Home screen and place the real, working
buttons (Start Game, Leaderboard, Instructions, Customize Character) on top,
aligned to your design — the same approach as the maze artwork.

## Tips
- Any size is fine, but a tall/portrait shape matches the app's phone-style frame
  best (roughly 2:3 — e.g. 1000×1500). Landscape works too; I'll fit it.
- If you've drawn the title/buttons into the image, that's fine — they'll be the
  look, and I'll put the real clickable buttons over them.
- If you'd rather the buttons NOT be baked in (just the background scene/logo),
  that's great too — I'll style the buttons to match.

Once `home-bg.png` is in this folder, tell me and I'll wire it up.
