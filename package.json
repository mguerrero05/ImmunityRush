{
  "name": "immunity-rush",
  "version": "1.0.0",
  "description": "Immunity Rush — a hospital-themed maze game about staying ahead of flu season",
  "private": true,
  "scripts": {
    "dev": "web-dev-server --watch --open --port 5678",
    "format": "prettier --write .",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@web/dev-server": "^0.4.6",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "prettier": "^3.9.4"
  }
}
